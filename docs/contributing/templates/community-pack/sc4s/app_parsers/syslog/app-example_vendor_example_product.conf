# Example parser scaffold for a community pack.
# Replace this placeholder with real SC4S/syslog-ng application/parser blocks.
# Keep functional config in .conf files rather than embedding it in pack.json.
